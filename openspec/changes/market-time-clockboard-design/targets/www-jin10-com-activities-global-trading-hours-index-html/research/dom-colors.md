# DOM / computed-style palette evidence

Captured 2026-08-01 from the reference page's inline root and dark-theme declarations returned by
HTTP 200. Browser navigation and title probing also succeeded (`金十数据 全球交易时间表`); the
page's heavy runtime timed out on a later bulk computed-style projection, so the safe manual
procedure used its authored CSS declarations plus the separately measured screenshot.

Dark-theme roles:

- page ground `#0F1117` (`--bg`)
- card `#181B23` (`--bg-card`)
- primary text `#E8EAF0` (`--t1`)
- current-time needle `#F08A63` (`--needle`)
- rule `#2A2E3C` (`--border`)

No reference CSS, data, copy, or assets are shipped.
