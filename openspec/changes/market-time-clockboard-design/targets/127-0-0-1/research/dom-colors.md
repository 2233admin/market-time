# DOM / computed-style palette evidence

Captured from the primary page's CSS custom properties and browser-visible implementation on
2026-08-01. The earlier primary capture used `frontend/assets/audit.css` (then the root page):

- page ground `#090D18` (`--mt-ink`)
- main panel `#101827` (`--mt-surface`)
- primary text `#E7EDF7` (`--mt-reference`)
- time cursor / control signal `#FFB35C` (`--mt-signal`)

These roles are adapted into the corrected timetable rather than copied as exact values.
